@using TravelTripProje.Models.Classes
@model List<Blog>

<div class="padding">
        <h4>En Popüler 10 Blog</h4>
        <ul class="list list-pad">
            @foreach (var item in Model)
            {
                <li class="list-bg"><a href="/Blog/BlogDetail/@item.Id">@item.Title</a></li>
            }

        </ul>
    <div class="clear"></div>

</div>
